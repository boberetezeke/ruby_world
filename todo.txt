* add tag context. This should allow:
** object creation so objects are automatically tagged
** allow objects that are brought in from the outside get tagged according to some context rules
** Listing of all objects that show up within a tag context
** Have command line respond to current tag context
